{% extends 'base.html.twig' %}

{% block title %}Hygiene{% endblock %}

{% block body %}
    <h1>Hygiene</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hygiene.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ hygiene.date ? hygiene.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ hygiene.heure ? hygiene.heure|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ hygiene.libelle }}</td>
            </tr>
            <tr>
                <th>Soins</th>
                <td>{{ hygiene.soins }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hygiene_index') }}">back to list</a>

    <a href="{{ path('hygiene_edit', {'id': hygiene.id}) }}">edit</a>

    {{ include('hygiene/_delete_form.html.twig') }}
{% endblock %}
